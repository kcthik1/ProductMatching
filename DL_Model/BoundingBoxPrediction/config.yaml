# Dataset
data_dir: "data/product_images"
annotation_file: "annotations.json"
train_split: 0.7
val_split: 0.15
test_split: 0.15

# Model
model_name: "fasterrcnn_resnet50_fpn"
num_classes: 2  # Background image and nutritional table

# Training
batch_size: 8
num_epochs: 100
learning_rate: 0.001
momentum: 0.9
weight_decay: 0.0005

# Inference
confidence_threshold: 0.7

# Paths
saved_model_path: "nutritional_table_detector.pth"
output_dir: "output"